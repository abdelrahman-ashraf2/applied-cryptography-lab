# Original college exercises

This directory preserves the four initial Information Security course scripts so the repository shows how the work evolved. The production-style package in `src/crypto_lab` adds input validation, reusable functions, a CLI, complete decryption round trips, documentation, and automated tests.

The original sample image is intentionally excluded because its redistribution rights are unclear.
